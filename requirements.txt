# Python dependencies.
# Stage 0 (feature extraction + estimators):
numpy
scipy
librosa
soundfile
scikit-learn
pandas
matplotlib
tqdm
# Stage 1-2 (config + LLM harness):
pyyaml              # load config/*.yaml
pretty_midi         # build MIDI motifs for the generator (rendered by FluidSynth)
# Ollama is called over HTTP with the stdlib (urllib) - no extra dependency.
anthropic           # OPTIONAL - only needed if you run the Sonnet cross-check